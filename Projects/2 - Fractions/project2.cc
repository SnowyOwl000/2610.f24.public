#include <iostream>

using namespace std;

// function for Stern-Brocot approximation goes here

// function to generate Egyptian fractions goes here

// and the main function
int main(int argc,char *argv[]) {

  // make sure user entered something on the command line
  if (argc != 2) {
    cout << "Usage: ./a.out number" << endl;
    return 1;
  }
  
  // convert command line argument to numeric form
  
  // call the Stern-Brocot function
  
  // call the Egyptian function
  
  // all done
  return 0;
}

